#include "dsc_inc_vars"

//==========================================/
int StartingConditional()
//==========================================/
    {
    return GetLocalInt(OBJECT_SELF, UI_CCI_DISP_CNT) > 9;
    }
