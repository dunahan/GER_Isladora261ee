#include "spawn_main"
#include "_area_inc"

void main()
{/*
  if (GetLocalInt(GetObjectByTag("MonsterSpawn"), "DebugMode") == 1
  ||  GetLocalInt(OBJECT_SELF, "NoSpawn")                      == 1 )
  { return; }

  Spawn();

  object oArea = GetArea(OBJECT_SELF);
  ExecuteScript(GetLocalString(oArea, "AreaHeartbeat"), oArea); */
}

